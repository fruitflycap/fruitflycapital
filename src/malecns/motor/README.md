# Motor population registry

`registry.py` exposes annotation/evidence-grounded descending candidates.
It returns empty results for stop or dedicated walking-state populations when
MaleCNS v1.0 does not expose those exact labels.
